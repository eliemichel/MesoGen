#pragma once

#ifdef _WIN32
#include <windows.h> // Avoid issue with APIENTRY redefinition in Glad
#endif // _WIN32

//#include <glad/glad.h>
#include <glad/modernglad.h> // Adds warning about obsolete non-DSA functions
